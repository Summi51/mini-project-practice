import React from 'react';

function Store() {
  return (
    <div>
      <h1>Store</h1>
      <p>This is a protected page, only accessible after login.</p>
      {/* Add more protected content, like cards, user info, etc. */}
    </div>
  );
}

export default Store;
